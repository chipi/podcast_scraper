name: chipi/podcast-scraper-codeql
disable-default-queries: false
queries:
  - uses: security-and-quality
# Model pack (#538) is in .github/codeql/extensions/ but not wired here until
# chipi/podcast-scraper-python-extensions is published to GHCR (init resolves
# packs only from the registry in matrix workflows).
