{# KG extraction from summary bullets only (no transcript). #}
Episode title: {{ title | default("", true) }}

The following lines are **episode summary bullets** (already distilled). From them only, derive up to {{ max_topics }} distinct **topics** as **short headings** (about **2–8 words**, noun-style themes — never a full bullet copied verbatim) and up to {{ max_entities }} **named entities** (people or organizations) that the bullets imply. Use **description** for extra nuance. Do not exceed these counts; put the strongest items first.

Summary bullets:
{% for b in bullets %}
- {{ b }}
{% endfor %}
