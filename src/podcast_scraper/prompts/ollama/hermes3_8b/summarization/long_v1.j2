{# Nous-native user prompt for Hermes 3. Wrapped automatically by Ollama into
   <|im_start|>user ... <|im_end|>. Style follows Nous's chat-template
   convention: state the task crisply, deliver the payload, restate the
   binding constraints once at the end so they sit nearest the assistant turn. #}
Summarize the following podcast episode transcript.

{% if title -%}
Episode title: {{ title }}

{% endif -%}
Transcript:
{{ transcript }}

Write a single coherent summary covering the episode's substantive content. Aim for {{ paragraphs_min }}–{{ paragraphs_max }} paragraphs.

Constraints (must hold):
- Abstractive and synthesized; no verbatim sentence copying.
- Focus on decisions, arguments, lessons, and insights that drove the conversation forward.
- Ignore ads, sponsorships, listener mail, and housekeeping segments.
- No speaker names. No direct quotations.
- Do not invent facts beyond what the transcript supports.
- Plain text only — no markdown, no headings, no bullet lists, no code blocks, no JSON.
- Begin directly with the summary content; no preamble or framing sentence.
