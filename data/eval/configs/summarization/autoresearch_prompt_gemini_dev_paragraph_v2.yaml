# Autoresearch v2 non-bundled paragraph ratchet — Gemini, dev dataset.

id: "autoresearch_prompt_gemini_dev_paragraph_v2"
task: "summarization"

backend:
  type: "gemini"
  model: "gemini-2.0-flash"

prompts:
  system: "gemini/summarization/system_v1"
  user: "gemini/summarization/long_v1"

data:
  dataset_id: "curated_5feeds_dev_v1"

params:
  max_length: 4000
  min_length: 200
  temperature: 0.0

preprocessing_profile: "cleaning_v4"
