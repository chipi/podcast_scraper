# Autoresearch v2 bundled paragraph — HELD-OUT benchmark validation (Anthropic).
# DO NOT iterate against this dataset.

id: "autoresearch_prompt_anthropic_bundled_benchmark_paragraph_v2"
task: "summarization"

backend:
  type: "anthropic"
  model: "claude-haiku-4-5-20251001"

llm_pipeline_mode: "bundled"

data:
  dataset_id: "curated_5feeds_benchmark_v2"

params:
  temperature: 0.0

preprocessing_profile: "cleaning_v4"
scoring_output_field: "summary"
