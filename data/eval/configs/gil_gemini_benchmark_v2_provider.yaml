# GI on held-out: Gemini provider mode (direct insight extraction from transcript).
# Optimized settings from autoresearch: n=12, source=provider.
id: gil_gemini_benchmark_v2_provider
task: grounded_insights

backend:
  type: gemini
  model: gemini-2.5-flash-lite

prompts:
  user: gemini/summarization/long_v1

data:
  dataset_id: curated_5feeds_benchmark_v2

preprocessing_profile: cleaning_v4

params:
  max_length: 800
  min_length: 200
  temperature: 0.0
  gi_insight_source: provider
  gi_max_insights: 12
  gi_require_grounding: true
