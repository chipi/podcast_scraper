# Autoresearch Track A — paragraph summaries, Gemini backend.
#
# Pair with silver: ``silver_gpt4o_smoke_v1`` (GPT-4o prose reference, reused for all providers).
#
# Template under tuning: src/podcast_scraper/prompts/gemini/summarization/long_v1.j2
# Results log: autoresearch/prompt_tuning/results_summary_gemini.tsv
#
# Requires: GEMINI_API_KEY

id: "autoresearch_prompt_gemini_benchmark_paragraph_v1"
task: "summarization"

backend:
  type: "gemini"
  model: "gemini-2.0-flash"

prompts:
  system: "gemini/summarization/system_v1"
  user: "gemini/summarization/long_v1"

data:
  dataset_id: "curated_5feeds_benchmark_v1"

params:
  max_length: 800
  min_length: 200
  temperature: 0.0

preprocessing_profile: "cleaning_v4"
