# Autoresearch v2 bundled bullets — HELD-OUT benchmark validation.
#
# DO NOT iterate against this dataset. Use only to validate champions
# that were accepted via the dev_v2 ratchet.
#
# Paired silver: silver_sonnet46_benchmark_v2_bullets (5 held-out e03 episodes, ~32 min each).

id: "autoresearch_prompt_openai_bundled_benchmark_bullets_v2"
task: "summarization"

backend:
  type: "openai"
  model: "gpt-4o"

llm_pipeline_mode: "bundled"

data:
  dataset_id: "curated_5feeds_benchmark_v2"

params:
  temperature: 0.0
  seed: 42

preprocessing_profile: "cleaning_v4"
scoring_output_field: "bullets"
