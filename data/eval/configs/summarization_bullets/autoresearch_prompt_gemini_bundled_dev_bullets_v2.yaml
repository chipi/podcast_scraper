# Autoresearch v2 bundled bullets ratchet — Gemini, dev dataset (10 episodes).
# Paired silver: silver_sonnet46_dev_v1_bullets
# Requires: GEMINI_API_KEY (or AUTORESEARCH_EXPERIMENT_GEMINI_API_KEY).

id: "autoresearch_prompt_gemini_bundled_dev_bullets_v2"
task: "summarization"

backend:
  type: "gemini"
  model: "gemini-2.0-flash"

llm_pipeline_mode: "bundled"

data:
  dataset_id: "curated_5feeds_dev_v1"

params:
  temperature: 0.0

preprocessing_profile: "cleaning_v4"
scoring_output_field: "bullets"
