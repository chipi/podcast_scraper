# Autoresearch — DGX-hosted Llama 3.3 70B (RFC-089). Requires OLLAMA_API_BASE on tailnet DGX.
#
# Pair with silver as needed. Model must be pre-pulled on DGX (pinned tag).

id: "autoresearch_prompt_ollama_llama33_70b_dgx_smoke_bullets_v1"
task: "summarization"

backend:
  type: "ollama"
  model: "llama3.3:70b-instruct"

prompts:
  system: "shared/summarization/system_bullets_v1"
  user: "shared/summarization/bullets_json_v1"

data:
  dataset_id: "curated_5feeds_smoke_v1"

params:
  max_length: 800
  min_length: 200
  temperature: 0.0

preprocessing_profile: "cleaning_v4"
