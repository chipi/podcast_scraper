# Dependency / build trees (markdownlint --ignore node_modules only matches ./node_modules)
**/node_modules/
**/.venv/

# Files with legitimate long lines (technical descriptions, URLs, etc.)
docs/ARCHITECTURE.md
docs/DEPENDENCIES.md
docs/DEVELOPMENT_GUIDE.md
docs/rfc/*.md
docs/prd/*.md
docs/releases/*.md

# Claude Code skill references under the viewer (upstream-style notes; not repo docs)
web/gi-kg-viewer/.claude/

# iOS release tooling artefacts, not repo docs:
#   - ios/vendor/** is `bundle install --path vendor/bundle`, i.e. third-party gem READMEs
#   - ios/fastlane/README.md is GENERATED by `fastlane docs` and rewritten on every lane change
# Both are untracked build output; linting them fails on prose we neither wrote nor may edit.
web/learning-player/ios/vendor/
web/learning-player/ios/fastlane/README.md
