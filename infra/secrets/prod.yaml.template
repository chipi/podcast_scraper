# ADR-115 secret template — podcast_scraper (tenant zero).
# Copy to prod.yaml, fill real values, encrypt to prod.enc.yaml (see README.md).
# NEVER commit the filled-in plaintext (infra/secrets/.gitignore blocks *.yaml).
# Each key becomes /run/secrets/podcast/<key> and, via the shim, the UPPERCASE
# env var the app reads (openai_api_key -> OPENAI_API_KEY).
openai_api_key: ""
gemini_api_key: ""
anthropic_api_key: ""
mistral_api_key: ""
deepseek_api_key: ""
grok_api_key: ""
podcast_sentry_dsn_api: ""
podcast_sentry_dsn_pipeline: ""
