# Tracked default for --from-fast-stems when config/acceptance/FAST_CONFIGS.txt is absent
# (e.g. branch without that file yet). One stem per line; # = comment.
# Stems match acceptance YAML filenames without .yaml under config/acceptance/full/ or config/examples/.

# ML dev baseline (local, no API keys, fastest)
acceptance_planet_money_ml_dev
acceptance_the_journal_ml_dev

# ML prod baseline (local, no API keys)
acceptance_planet_money_ml_prod

# One LLM provider (USE_FIXTURES=1 rewrites RSS + API bases to E2E mocks)
acceptance_planet_money_openai
acceptance_the_journal_openai

# One Ollama model (local, no API keys)
acceptance_planet_money_ollama

# Multi-feed corpus (two feeds → fixture URLs under USE_FIXTURES=1)
acceptance_multi_feed_planet_money_journal_openai
