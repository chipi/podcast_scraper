# Accepted ungoverned-divergence baseline — profile_governance_audit.py --check ratchets on this.
#
# Every entry is a Config field that profiles hand-author DIFFERENTLY and the registry does not
# govern. Being here means a human SIGNED the divergence. Adding an entry requires a reason;
# a stale entry (no longer divergent, or since governed) fails the check so the file stays honest.
#
# Seeded 2026-08-30 from the first audit run (#1878 arc): these 60+ divergences pre-date the
# ratchet. Each is presumed-intentional per-profile tuning until touched — REVIEW the entry's
# field when you next edit it, and either govern it or replace this seed reason with a real one.
accepted:
  anthropic_speaker_model: "pre-ratchet divergence, seeded 2026-08-30 — review when next touched (see #1878)"
  anthropic_summary_model: "pre-ratchet divergence, seeded 2026-08-30 — review when next touched (see #1878)"
  audio_evict_local_after_offload: "pre-ratchet divergence, seeded 2026-08-30 — review when next touched (see #1878)"
  audio_preprocessing_profile: "pre-ratchet divergence, seeded 2026-08-30 — review when next touched (see #1878)"
  audio_remote_base_path: "pre-ratchet divergence, seeded 2026-08-30 — review when next touched (see #1878)"
  audio_remote_rclone_remote: "pre-ratchet divergence, seeded 2026-08-30 — review when next touched (see #1878)"
  audio_storage_backend: "pre-ratchet divergence, seeded 2026-08-30 — review when next touched (see #1878)"
  cost_daily_alert_usd: "pre-ratchet divergence, seeded 2026-08-30 — review when next touched (see #1878)"
  cost_soft_cap_action: "pre-ratchet divergence, seeded 2026-08-30 — review when next touched (see #1878)"
  cost_soft_cap_usd_per_run: "pre-ratchet divergence, seeded 2026-08-30 — review when next touched (see #1878)"
  deepseek_cleaning_model: "cloud_balanced still runs deepseek-chat on this fallback sibling — flagged in #1878 checklist, do not silently 'fix' prod"
  deepseek_extra_body: "pre-ratchet divergence, seeded 2026-08-30 — review when next touched (see #1878)"
  deepseek_speaker_model: "same as deepseek_cleaning_model — #1878 checklist"
  deepseek_summary_model: "prod-relevant profiles pinned to v4-flash 2026-08-30; bakeoff/airgapped intentionally vary"
  degradation_policy: "pre-ratchet divergence, seeded 2026-08-30 — review when next touched (see #1878)"
  dgx_diarize_request_timeout_sec: "genuinely per-profile: only the DGX-diarizing profiles set it, and prod_dgx_full needs 900 (not the 180 default) because `soundfile` is absent from the pipeline image, so the duration probe returns None and every episode gets a flat timeout regardless of length. Folded in from a box override 2026-08-30 after ~45 long diarizations with zero recurrences. Drops back toward the default once the duration probe has an ffprobe fallback"
  dgx_tailnet_host: "pre-ratchet divergence, seeded 2026-08-30 — review when next touched (see #1878)"
  diarization_provider: "pre-ratchet divergence, seeded 2026-08-30 — review when next touched (see #1878)"
  diarize: "pre-ratchet divergence, seeded 2026-08-30 — review when next touched (see #1878)"
  enrichment: "pre-ratchet divergence, seeded 2026-08-30 — review when next touched (see #1878)"
  entailment_provider: "pre-ratchet divergence, seeded 2026-08-30 — review when next touched (see #1878)"
  gemini_cleaning_model: "pre-ratchet divergence, seeded 2026-08-30 — review when next touched (see #1878)"
  gemini_speaker_model: "pre-ratchet divergence, seeded 2026-08-30 — review when next touched (see #1878)"
  gemini_summary_model: "pre-ratchet divergence, seeded 2026-08-30 — review when next touched (see #1878)"
  generate_gi: "pre-ratchet divergence, seeded 2026-08-30 — review when next touched (see #1878)"
  generate_kg: "pre-ratchet divergence, seeded 2026-08-30 — review when next touched (see #1878)"
  generate_metadata: "pre-ratchet divergence, seeded 2026-08-30 — review when next touched (see #1878)"
  generate_summaries: "pre-ratchet divergence, seeded 2026-08-30 — review when next touched (see #1878)"
  gi_typed_mentions_use_ner: "pre-ratchet divergence, seeded 2026-08-30 — review when next touched (see #1878)"
  groq_cleaning_model: "pre-ratchet divergence, seeded 2026-08-30 — review when next touched (see #1878)"
  groq_extra_body: "pre-ratchet divergence, seeded 2026-08-30 — review when next touched (see #1878)"
  groq_insight_model: "pre-ratchet divergence, seeded 2026-08-30 — review when next touched (see #1878)"
  groq_speaker_model: "pre-ratchet divergence, seeded 2026-08-30 — review when next touched (see #1878)"
  groq_summary_model: "pre-ratchet divergence, seeded 2026-08-30 — review when next touched (see #1878)"
  kg_extraction_provider: "pre-ratchet divergence, seeded 2026-08-30 — review when next touched (see #1878)"
  kg_extraction_use_ner_prepass: "deployment fact, not tuning: profiles running on the pipeline image (prod_dgx_full, cloud_balanced, cloud_thin) declare false because that image ships no spaCy — the true default was degrading to the v4 prompt via ImportError on every prod episode. Local/dev profiles keep the default true because they DO have spaCy. Measured 2026-08-30 on 60 prod episodes without the pre-pass: mean 10.5 entities, zero zero-entity episodes. Re-evaluate under #1889, and revisit if KG moves to a smaller local model (#1035)"
  kg_organizations_use_ner: "pre-ratchet divergence, seeded 2026-08-30 — review when next touched (see #1878)"
  kg_topic_corpus_clustering: "pre-ratchet divergence, seeded 2026-08-30 — review when next touched (see #1878)"
  litellm_api_base: "pre-ratchet divergence, seeded 2026-08-30 — review when next touched (see #1878)"
  litellm_cleaning_model: "pre-ratchet divergence, seeded 2026-08-30 — review when next touched (see #1878)"
  litellm_extra_body: "pre-ratchet divergence, seeded 2026-08-30 — review when next touched (see #1878)"
  litellm_insight_model: "pre-ratchet divergence, seeded 2026-08-30 — review when next touched (see #1878)"
  llm_retry_initial_delay_seconds: "pre-ratchet divergence, seeded 2026-08-30 — review when next touched (see #1878)"
  llm_retry_max_delay_seconds: "pre-ratchet divergence, seeded 2026-08-30 — review when next touched (see #1878)"
  llm_retry_max_retries: "pre-ratchet divergence, seeded 2026-08-30 — review when next touched (see #1878)"
  mistral_speaker_model: "pre-ratchet divergence, seeded 2026-08-30 — review when next touched (see #1878)"
  mistral_summary_model: "pre-ratchet divergence, seeded 2026-08-30 — review when next touched (see #1878)"
  openai_cleaning_model: "pre-ratchet divergence, seeded 2026-08-30 — review when next touched (see #1878)"
  openai_speaker_model: "pre-ratchet divergence, seeded 2026-08-30 — review when next touched (see #1878)"
  openai_summary_model: "pre-ratchet divergence, seeded 2026-08-30 — review when next touched (see #1878)"
  quote_extraction_provider: "pre-ratchet divergence, seeded 2026-08-30 — review when next touched (see #1878)"
  qwen_api_base: "pre-ratchet divergence, seeded 2026-08-30 — review when next touched (see #1878)"
  qwen_api_key_env: "pre-ratchet divergence, seeded 2026-08-30 — review when next touched (see #1878)"
  qwen_cleaning_model: "pre-ratchet divergence, seeded 2026-08-30 — review when next touched (see #1878)"
  qwen_extra_body: "pre-ratchet divergence, seeded 2026-08-30 — review when next touched (see #1878)"
  qwen_insight_model: "pre-ratchet divergence, seeded 2026-08-30 — review when next touched (see #1878)"
  qwen_verify_served_model: "pre-ratchet divergence, seeded 2026-08-30 — review when next touched (see #1878)"
  summarization_timeout: "pre-ratchet divergence, seeded 2026-08-30 — review when next touched (see #1878)"
  summary_mode_id: "pre-ratchet divergence, seeded 2026-08-30 — review when next touched (see #1878)"
  summary_reduce_model: "pre-ratchet divergence, seeded 2026-08-30 — review when next touched (see #1878)"
  summary_reduce_params: "pre-ratchet divergence, seeded 2026-08-30 — review when next touched (see #1878)"
  transcript_cache_enabled: "pre-ratchet divergence, seeded 2026-08-30 — review when next touched (see #1878)"
  transcript_cleaning_strategy: "pre-ratchet divergence, seeded 2026-08-30 — review when next touched (see #1878)"
  transcription_fallback_provider: "pre-ratchet divergence, seeded 2026-08-30 — review when next touched (see #1878)"
  transcription_timeout: "pre-ratchet divergence, seeded 2026-08-30 — review when next touched (see #1878)"
  vector_search: "pre-ratchet divergence, seeded 2026-08-30 — review when next touched (see #1878)"
  whisper_model: "pre-ratchet divergence, seeded 2026-08-30 — review when next touched (see #1878)"
