# LEGACY stub (pre-2.6) — superseded by config/profiles/freeze/*.yaml.
# Kept for one release cycle while older docs/links migrate.
#
# Preferred workflow:
#   make profile-freeze VERSION=vX.Y.Z \
#     PIPELINE_CONFIG=config/profiles/freeze/<provider>.yaml
#
# The freeze/ profiles pair with freeze/_defaults.yaml for operational fields
# (rss, output_dir, max_episodes, whisper_device). See config/profiles/README.md
# and config/profiles/freeze/README.md.

rss_url: "https://example.com/replace-with-your-feed.xml"
output_dir: ".tmp/profile_freeze_run"
max_episodes: 2
log_level: "INFO"
transcribe_missing: false
generate_metadata: true
generate_summaries: false
auto_speakers: false
