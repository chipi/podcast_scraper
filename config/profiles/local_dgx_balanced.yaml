# Profile: local_dgx_balanced — DGX-hosted Ollama with cloud degradation fallback (RFC-089)
#
# Point ollama_api_base / dgx_tailnet_host at your DGX MagicDNS name before running.
# See docs/guides/DGX_RUNBOOK.md.

dgx_tailnet_host: dgx-llm-1.tail6d0ed4.ts.net
ollama_api_base: http://dgx-llm-1.tail6d0ed4.ts.net:11434/v1

transcription_provider: whisper
whisper_model: small.en

# Screenplay + neural diarization (RFC-058)
screenplay: true
diarize: true

audio_preprocessing_profile: speech_optimal_v1

speaker_detector_provider: spacy
ner_model: en_core_web_trf
auto_speakers: true

summary_provider: ollama
ollama_summary_model: qwen3.5:9b
llm_pipeline_mode: bundled

generate_gi: true
gi_insight_source: provider
gi_max_insights: 12
gi_require_grounding: true

generate_kg: true
kg_extraction_source: provider
kg_max_topics: 10
kg_max_entities: 15

vector_search: true
vector_backend: faiss
# ADR-098 / #897: DGX profiles serve embeddings via Ollama (one service on DGX
# instead of two). nomic-embed-text: 8192-token context, ~62.4 MTEB avg.
# DGX_TAILNET_FQDN is substituted by the operator (see DGX_RUNBOOK §P1).
vector_embedding_provider: ollama
vector_embedding_model: nomic-embed-text
vector_embedding_endpoint: http://${DGX_TAILNET_FQDN}:11434

degradation_policy:
  fallback_provider_on_failure: gemini
  continue_on_stage_failure: true

generate_metadata: true
generate_summaries: true
language: en
log_level: INFO
