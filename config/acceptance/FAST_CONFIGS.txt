# Config stems for --from-fast-stems / test-acceptance-fixtures-fast (CI).
# One stem per line (filename without .yaml). Each must exist as
# config/acceptance/<stem>.yaml (tracked in Git).
#
# Use sample_acceptance_e2e_fixture_* only: full Planet Money / Journal YAMLs are
# optional/local; fixture presets work with USE_FIXTURES=1 + mock RSS/APIs.
#
# Ollama-based sample_* stems are omitted here so CI stays offline without a daemon;
# run them locally: make test-acceptance CONFIGS=... USE_FIXTURES=1

# Single-feed ML (local models)
sample_acceptance_e2e_fixture_single
sample_acceptance_e2e_fixture_multi_ml

# Multi-feed + LLM-shaped presets: under USE_FIXTURES=1 (e.g. make test-acceptance-fixtures-fast)
# the runner rewrites feeds and points provider HTTP at the in-process E2E mock server — no
# paid API calls and no API keys required on CI.
sample_acceptance_e2e_fixture_single_openai
sample_acceptance_e2e_fixture_multi_openai
sample_acceptance_e2e_fixture_single_mistral
sample_acceptance_e2e_fixture_multi_mistral
sample_acceptance_e2e_fixture_single_anthropic
sample_acceptance_e2e_fixture_multi_anthropic
sample_acceptance_e2e_fixture_single_deepseek
sample_acceptance_e2e_fixture_multi_deepseek
