# Manual validation: NPR Planet Money, OpenAI — GI + KG driven by summary bullets (recommended first GI/KG pass).
# Insight texts and KG topics come from OpenAI summary bullets; no separate LLM insight/graph JSON calls.
#
# Run:
#   python -m podcast_scraper.cli --config config/manual/manual_planet_money_openai_gi_kg_summary_bullets.yaml
# Requires: OPENAI_API_KEY

rss: https://feeds.npr.org/510289/podcast.xml
output_dir: .test_outputs/manual/planet_money_openai_gi_kg_summary_bullets
max_episodes: 3

transcription_provider: openai
speaker_detector_provider: openai
summary_provider: openai

openai_transcription_model: whisper-1
openai_speaker_model: gpt-4o-mini
openai_summary_model: gpt-4o-mini

transcribe_missing: true
preprocessing_enabled: true

generate_metadata: true
generate_summaries: true

generate_gi: true
gi_insight_source: summary_bullets
gi_max_insights: 5
gi_insight_model: gpt-4o-mini

generate_kg: true
kg_extraction_source: summary_bullets
kg_max_topics: 5
