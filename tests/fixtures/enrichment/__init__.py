"""Shared enrichment-layer test fixtures (mock scorers + a tiny enricher zoo).

Tests import via ``from tests.fixtures.enrichment.mock_scorers import ...`` —
these mocks exercise the resilience pipeline without loading real models.
"""
