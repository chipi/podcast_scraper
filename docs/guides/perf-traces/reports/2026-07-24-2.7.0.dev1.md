# Perf-trace report — 2026-07-24 · 2.7.0.dev1

First unified per-release perf report under the resolidified framework
([../index.md](../index.md)). Establishes the **2.7.0.dev1 baseline** across the
Search, Graph, and Surfaces harnesses. No prior unified release to diff against,
so the Δ columns open next release.

- **Corpus:** `.test_outputs/manual/prod-v2/corpus` (reindexed to
  LANCE_SCHEMA_VERSION 3 — carries `insight_type`).
- **Conditions:** headless Chromium, 1440×900 @ DPR-2 (UI); localhost api (API).
- **Code:** branch `search-and-player-enhancement`; package `2.7.0.dev0` → this
  work ships in the `2.7.0.dev1` train.

## Search — UI

`data/perf/traces/search/2.7.0.dev1-search-ui.ui.metrics.json`

| Scenario | Metric | This release | Δ vs prev |
| --- | --- | --- | --- |
| workspace-open | tti_ms | 1012 | — |
| filter-apply | open_ms | 37 | — |
| results-paint | paint_ms | 4313 | — |
| cmdk-open | open_ms | 3 | — |
| operator-cluster | run_ms | 26 | — |
| operator-compare | run_ms | 808 | — |

`results-paint` (4.3 s) is the standout — the full submit→first-card path on a
99-episode corpus, dominated by the two-tier hybrid retrieval. `operator-compare`
(808 ms) is the S8 endpoint round-trip (two `build_briefing_pack` passes). Watch
these two next release.

## Graph — API

`data/perf/traces/graph/2.7.0.dev1-graph-api.api.metrics.json`

| Scenario | p50_ms | p95_ms | n | sigsegv_free |
| --- | --- | --- | --- | --- |
| api-artifacts-list | 108 | 109 | 3 | — |
| api-artifact-fetch | 0 | 0 | 60 | — |
| api-topic-clusters | 0 | 1 | 3 | — |
| api-concurrent-4 | 1 | 1 | 60 | true |

Server-side artifact serving is ~0 ms; the artifacts-list (108 ms) is the only
notable server cost. Confirms the graph-v3 finding: graph time-to-canvas is a
**client-side** parse/merge/fcose cost, not server latency.

## Surfaces — UI

`data/perf/traces/surfaces/2.7.0.dev1-surfaces.ui.metrics.json`

| Scenario | Metric | This release | Δ vs prev |
| --- | --- | --- | --- |
| library-load | tti_ms | 39 | — |
| digest-load | tti_ms | 16 | — |
| entity-load | open_ms | 20 | — |

Tab-switch / interaction → container-visible (corpus envelope pre-loaded on
landing). All snappy; these are the regression tripwires for the "boring"
surfaces.

## NOT captured this run

Equal-weight honesty section (framework requires it):

- **Search — API** (`capture-search-api`): not re-captured. The committed
  `S4-shell-fixture-baseline.api.metrics.json` is against the viewer-validation
  fixture, not prod-v2; its finance-query set doesn't match this corpus. A
  prod-v2 query set is needed for a comparable API baseline. Follow-up.
- **Graph — UI / LCP** (`capture-graph-lcp`): not re-captured this release; the
  standing reference is the [graph-v3 tuning report](graph-v3-tuning-2026-07-19.md)
  (`main` shell LCP ~928 ms, time-to-canvas ~5.8 s). Re-capture next release to
  start a live Δ series.
- **Absolute-latency caveat:** every number here is a scripted deterministic
  settle on one machine — good for cross-release *relative* comparison,
  indicative (not definitive) for real-user latency.
