# Core dependencies only - for full install use: pip install -e .[ml]
# Or for development: pip install -e .[dev,ml,docs]
# Version pinning: Using compatible release ranges to prevent breaking changes
# while allowing minor updates and security patches
#
# Note: rouge-score (required for scripts/eval_summaries.py) is included in
# dev optional dependencies. Install with: pip install -e .[dev]
requests>=2.31.0,<3.0.0
urllib3>=2.6.0,<3.0.0
tqdm>=4.66.0,<5.0.0
defusedxml>=0.7.1,<1.0.0
platformdirs>=3.11.0,<5.0.0
pydantic>=2.6.0,<3.0.0
PyYAML>=6.0,<7.0.0
types-PyYAML>=6.0.12,<7.0.0
python-dotenv>=1.0.0,<2.0.0  # For .env file support (RFC-013)
zipp>=3.19.1,<4.0.0  # Security fix: CVE-2024-50208 (infinite loop vulnerability)
jinja2>=3.1.0,<4.0.0  # For prompt templating (RFC-017)
openai>=1.0.0,<2.0.0  # For OpenAI API providers (RFC-013)
